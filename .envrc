# Activate the uv-managed venv automatically on cd.
# This adds .venv/bin/ to PATH so any installed project scripts
# (e.g. `tiny-search`) are available directly without `uv run`.
source .venv/bin/activate
